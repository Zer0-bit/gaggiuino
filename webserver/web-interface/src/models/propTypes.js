import PropTypes from 'prop-types';
import { Phase, Profile } from './profile';

export const ProfilePropType = PropTypes.instanceOf(Profile);
export const PhasePropType = PropTypes.instanceOf(Phase);
